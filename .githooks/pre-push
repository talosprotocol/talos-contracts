#!/usr/bin/env bash
set -euo pipefail

if [ -d typescript ]; then
  cd typescript
  npm test
  cd ..
fi

if [ -d python ]; then
  cd python
  pip install -e . -q 2>/dev/null || true
  pytest
  cd ..
fi
